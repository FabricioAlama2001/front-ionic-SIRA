import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yavirac.attendance',
  appName: 'Registro Asistencia',
  webDir: 'www'
};

export default config;
